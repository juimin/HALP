language: go

sudo: false

go:
  - 1.9.X

before_install: 
  - go get github.com/mattn/goveralls

env:
  - ADDR=":443" TLSCERT="$HOME/gopath/src/github.com/JuiMin/HALP/server/gateway/tls/fullchain.pem" TLSKEY="$HOME/gopath/src/github.com/JuiMin/HALP/server/gateway/tls/privkey.pem" COVERALLS_TOKEN="pXG7s1nhA2iTD6qlq7P1A964rFmZetMRM"

script:
  - $GOPATH/bin/goveralls -service=travis-ci